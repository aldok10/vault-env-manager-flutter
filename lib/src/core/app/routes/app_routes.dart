abstract class AppRoutes {
  static const String initial = '/'; // This is now AuthPage, but we keep it
  static const String loader = '/loader';
  static const String workbench = '/workbench';
  static const String settings = '/settings';
  static const String vaultAuth = '/vault-login';
}
