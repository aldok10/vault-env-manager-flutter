---
description: Unified workflow for updating AGENT.md and repository governance.
---

1. **Skill Sync**:
   - Study latest contents in `.skills/` to identify advanced patterns (e.g., V2 AI).

2. **Protocol Refinement**:
   - Update `AGENT.md` with new operational rules.
   - Update `PROJECT_SKILL.md` with new architectural standards.

3. **Workflow Evolution**:
   - Update all `.agent/workflows/*.md` files to match the new protocol.
   - Ensure `// turbo` annotations are correctly placed.

// turbo
4. **Finalization**:
   - Update `AGENT_STATE.md` to reflect the new Agent Version (e.g., V2.0).
   - Run `flutter analyze` to ensure documentation links are intact.
